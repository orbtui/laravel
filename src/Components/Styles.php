<?php

namespace OrbtUI\Components;

use OrbtUI\OrbtUI;

class Styles extends OrbtUI {

    public function __construct()
    {

        // $config = config('orbtui.default_colors');

        // if (empty($default_colors)) {
        //     $config = require(__DIR__.'/../../config/orbtui.php');
        // }

        // if (array_key_exists('default_colors', $config)) {

        //     $colors = $config['default_colors'];

        //     foreach ($colors as $name => $color) {
        //         if (in_array($name, $this->defaultColors)) {
        //             $this->css()->add($name, $color);
        //         }
        //     }

        // }

    }

}
